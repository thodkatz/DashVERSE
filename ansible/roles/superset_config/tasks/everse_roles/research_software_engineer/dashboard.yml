- name: Create Research Software Engineer dashboard
  uri:
    url: "{{ superset_api }}/dashboard/"
    method: POST
    headers:
      Cookie: "{{ session_cookie }}"
      X-CSRFToken: "{{ csrf_token }}"
      Content-Type: application/json
    body_format: json
    body:
      dashboard_title: "Research Software Engineer"
      slug: "research-software-engineer"
      published: true
      json_metadata: |
        {
          "expanded_slices": {},
          "refresh_frequency": 0,
          "color_scheme": "supersetColors",
          "label_colors": {},
          "shared_label_colors": {},
          "cross_filters_enabled": true,
          "default_filters": "{}"
        }
    status_code: [200, 201]
  when: existing_dashboards.json.result | selectattr('slug', 'equalto', 'research-software-engineer') | list | length == 0
  register: rse_created
